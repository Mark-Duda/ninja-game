using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WallGrab : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
       // Vector2 rayStartLeft = new Vector2(playerCollider.bounds.min.x, playerCollider.bounds.center.y);
       // Vector2 rayStartRight = new Vector2(playerCollider.bounds.max.x, playerCollider.bounds.center.y);

        // Cast rays horizontally to check if the player is grounded on each side
     //   RaycastHit2D hitLeft = Physics2D.Raycast(rayStartLeft, Vector2.left, raycastLength, groundLayer);
        //RaycastHit2D hitRight = Physics2D.Raycast(rayStartRight, Vector2.right, raycastLength, groundLayer);
    }
}
